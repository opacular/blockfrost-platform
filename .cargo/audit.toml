[advisories]
ignore = [
  "RUSTSEC-2024-0436", # paste unmaintained (transitive via pallas-traverse/binary-layout)
  "RUSTSEC-2025-0124", # rand_os unmaintained (transitive via cardano-serialization-lib)
]
informational_warnings = ["unmaintained"]
severity_threshold = "low"

[output]
deny = ["unmaintained"]
format = "terminal"
quiet = false
show_tree = true

[yanked]
enabled = true
update_index = true
